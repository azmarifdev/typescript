"use strict";
const arrayOfNumbers = [1, 2, 3];
const arrayOfStrings = arrayOfNumbers.map((number) => number.toString());
const Area = {
    height: 10,
    width: 12,
};
Area.height = 15;
const AreaRead = {
    height: 10,
    width: 12,
};
const area = {
    height: 56,
    width: 98,
    depth: 78,
};
// area.depth = 88
